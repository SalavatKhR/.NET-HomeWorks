@model UserProfile

@using (Html.BeginForm())
{
     <div class="form-group"> 
         @Html.ValidationSummary()  
     </div> 
    <div class="form-group">
        @Html.MyEditorForModel()
    </div>
}